<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bluemsun.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.bluemsun.entity.User">
            <id property="userId" column="user_id" jdbcType="INTEGER"/>
            <result property="username" column="username" jdbcType="VARCHAR"/>
            <result property="password" column="password" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        user_id,username,`password`
    </sql>

    <insert id="insert" keyColumn="user_id" keyProperty="userId" parameterType="com.bluemsun.entity.User" useGeneratedKeys="true">
        insert into tb_user
        (username,password)
        values (#{username},#{password})
    </insert>

    <update id="updatePassword" parameterType="User">
        update tb_user
        set
            `password` =  #{password}
        where   username = #{username}
    </update>


    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from tb_user
        where  user_id = #{userId}
    </select>

    <select id="selectOne" parameterType="User" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from tb_user
        where  username = #{username} and `password` = #{password}
    </select>

</mapper>
